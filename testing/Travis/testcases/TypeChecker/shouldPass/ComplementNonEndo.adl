CONTEXT ComplementNonEndoCheck IN ENGLISH

PURPOSE CONTEXT ComplementNonEndoCheck
{+ This test must succeed because complement always succeeds (no type constraints). +}

RELATION t[A*C]
RELATION result[A*C]

RULE testComplementNonEndo: result = -t

ENDCONTEXT
