CONTEXT Try32 IN ENGLISH
clientName[Client*Name] [UNI,TOT]
ENDCONTEXT

{- This script failed in revision 1069, when called with --haskell.
   For this reason, it is in the Try-sequence.
   Of course, this should pass.
-}