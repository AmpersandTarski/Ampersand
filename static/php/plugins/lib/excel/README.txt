This library enables importing database contents
that is located in an Excel spread
into a database
from within an Ampersand application.

This stuff is currently under development.
The PHP code won't work because the XLSX file that it imports has been kicked out of the repository
because it gives problems when generating a (new) prototype engine (which currently includes all files in the 'static' directory into itself)