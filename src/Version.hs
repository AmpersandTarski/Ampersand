{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-446" -- the number behind the last digit is the SVN revision number.

{- SVN Version text:
Check op unieke Service labels / SQL veldnamen ingebouwd. Sibling labels en velden moeten een unieke naam hebben. -}
