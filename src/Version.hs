{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-263" -- the number behind the last digit is the SVN revision number.

{- SVN Version text:
ALLE warnings gehaald uit de modules in de map Adl.
-}