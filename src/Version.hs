{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-354" -- the number behind the last digit is the SVN revision number.

{- SVN Version text:
SJ: Variant van makeTree toegevoegd in Classification.hs
-}
