{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-542" -- the number behind the last digit is the SVN revision number.

{-
SVN Version text:
Een tamelijk ernstige fout verholpen in de procesalgebra-normalisator (normPA).
-}
