{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-477" -- the number behind the last digit is the SVN revision number.

{- SVN Version text:
Opties voor genereren van verschillende output formaten m.b.t. functionele specificatie in ere hersteld, als aanzet voor generatie in verschillende formaten.
-}
