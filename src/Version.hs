{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-551" -- the number behind the last digit is the SVN revision number.

{-
SVN Version text:
Violations en multrules toegevoegd aan viewpoint. 
-}
