{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-431" -- the number behind the last digit is the SVN revision number.

{- SVN Version text:
Some small mistakes corrected in de code generator (RelBinGen and Object)...
-}
