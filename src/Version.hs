{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-572" -- the number behind the last digit is the SVN revision number.

{-
SVN Version text:
Database naam defaults nu naar de basename.
-}
