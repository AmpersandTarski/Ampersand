{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-285" -- the number behind the last digit is the SVN revision number.

{- SVN Version text:
Aanpassing aan options: Per optie kan nu worden aangegeven of die zichtbaar is voor de gebruiker of niet.
-}