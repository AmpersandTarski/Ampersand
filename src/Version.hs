{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-507" -- the number behind the last digit is the SVN revision number.

{-
SVN Version text:
Fout in LaTeX opgelost over de redirection van plaatjes.
-}
