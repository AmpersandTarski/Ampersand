{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-251" -- the number behind the last digit is the SVN revision number.

{- SVN Version text:
Gerard, ik heb even gekeken naar je typechecker.
-}