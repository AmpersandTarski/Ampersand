{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-492" -- the number behind the last digit is the SVN revision number.

{-
SVN Version text:
Special select query implemented for (Atlas) context expression
-}
