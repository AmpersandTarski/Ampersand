{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-395" -- the number behind the last digit is the SVN revision number.

{- SVN Version text:
Diverse SQL bugs van OUNL opgelost (nog niet alle)
-}
