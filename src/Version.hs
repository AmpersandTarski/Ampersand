{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-455" -- the number behind the last digit is the SVN revision number.

{- SVN Version text:
Namespaces in PHP uit elkaar gehaald door prefix met _ op de gebruikers-variabelen.  -}
