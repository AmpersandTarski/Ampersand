{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-580" -- the number behind the last digit is the SVN revision number.

{-
SVN Version text:
Nieuwe type checker op basis van uuag.
-}
