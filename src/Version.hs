{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-465" -- the number behind the last digit is the SVN revision number.

{- SVN Version text:
De Fspec2Pandoc is in de steigers gezet voor wat betreft de invulling van services. (werk in uitvoering)
Meer tekst wordt in Nederlands en Engels gegenereerd op basis van Options.
-}