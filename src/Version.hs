{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-467" -- the number behind the last digit is the SVN revision number.

{- SVN Version text:
Atlas genereert en presenteert plaatjes over de hele context.
-}
