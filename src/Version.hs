{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-514" -- the number behind the last digit is the SVN revision number.

{-
SVN Version text:
Error meldingen aangebracht daar waar werd vertrouwd op prelude undefined. (had ik last van...)
-}
