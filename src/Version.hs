{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-524" -- the number behind the last digit is the SVN revision number.

{-
SVN Version text:
ShowHS verbeterd.
Kleine bugs uit de typechecker hersteld.
-}
