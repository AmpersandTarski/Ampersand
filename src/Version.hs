{-# OPTIONS_GHC -Wall #-}
module Version (versionbanner)
where

versionbanner :: String
versionbanner = "ADL vs. 0.8.10-358" -- the number behind the last digit is the SVN revision number.

{- SVN Version text:
nieuwe versie 'Graphics.hs', zodat we over kunnen op de nieuwe graphviz
-}
