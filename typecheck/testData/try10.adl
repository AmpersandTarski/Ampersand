CONTEXT Test
-- should yield an error
PATTERN Test

r :: A*A
r :: B*C

RULE r[A*A] = r\/I

ENDPATTERN
ENDCONTEXT

{- This script is written as a superficial check whether type deduction works.
-}