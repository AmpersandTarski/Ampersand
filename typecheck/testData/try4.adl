CONTEXT Test

PATTERN Test4
RELATION r[X*Y]
RELATION s[X*A]
RELATION t[B*Y]
SPEC B ISA E
SPEC A ISA E
SPEC D ISA B
SPEC D ISA A
RULE r = s;t
ENDPATTERN

-- Result: FAIL
-- Reason: the intersection between A and B does not exist as a type.
-- If s;q were to be computed, the concepts at the connection at the ; are A, B, and E.
-- However, there is no way to choose between these concepts, because there is no smallest.

ENDCONTEXT